# from enum import Enum
from enum import Enum, auto
from random import random


class Skill(Enum):
    BIOLOGY = auto()
    CHEMISTRY = auto()
    MATH = auto()
    ENGLISH = auto()
    HISTORY = auto()

